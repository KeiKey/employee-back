#MySQL configuration for laptop - User Kei
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/support_portal
    username: root
    password: admin

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
